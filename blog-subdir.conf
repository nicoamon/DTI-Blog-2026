# Serve WordPress (root docroot) under the /blog URL path so it can live at
# dtisolution.id/blog while files stay at /var/www/html.
Alias /blog /var/www/html
<Directory /var/www/html>
    AllowOverride All
    Require all granted
</Directory>
